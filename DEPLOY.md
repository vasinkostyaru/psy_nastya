# 🚀 Инструкция по публикации на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите на "+" в правом верхнем углу → "New repository"
3. Введите название: `psycard` (или любое другое)
4. Выберите "Public" (для бесплатного GitHub Pages)
5. **НЕ** ставьте галочки "Add README" и "Add .gitignore"
6. Нажмите "Create repository"

## Шаг 2: Загрузите файлы

### Вариант A: Через терминал (рекомендуется)

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Первая версия сайта-визитки"

# Подключение к GitHub (замените YOUR-USERNAME и psycard на свои)
git remote add origin https://github.com/YOUR-USERNAME/psycard.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### Вариант B: Через веб-интерфейс GitHub

1. На странице нового репозитория нажмите "uploading an existing file"
2. Перетащите все файлы из папки проекта (кроме `.git` если есть)
3. Нажмите "Commit changes"

## Шаг 3: Активируйте GitHub Pages

1. Перейдите в репозиторий на GitHub
2. Нажмите "Settings" (⚙️)
3. В левом меню выберите "Pages"
4. В разделе "Source":
   - Branch: выберите `main`
   - Folder: выберите `/ (root)`
5. Нажмите "Save"
6. Подождите 1-2 минуты

## 🎉 Готово!

Ваш сайт доступен по адресу:
```
https://YOUR-USERNAME.github.io/psycard/
```

Замените `YOUR-USERNAME` на ваш username в GitHub.

## 📝 Обновление сайта

После внесения изменений в файлы:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт обновится автоматически через 1-2 минуты.

## ⚠️ Возможные проблемы

**Проблема**: Сайт не открывается после активации GitHub Pages

**Решение**:
- Подождите 5-10 минут
- Проверьте, что Branch установлен в `main`
- Откройте страницу в режиме инкогнито (Ctrl/Cmd + Shift + N)

**Проблема**: Изображение не отображается

**Решение**:
- Убедитесь, что файл `assets/photo.svg` загружен
- Проверьте путь в HTML: `<img src="assets/photo.svg">`
- Очистите кэш браузера

## 🔗 Собственный домен (опционально)

Если хотите использовать свой домен (например, `psychologist.ru`):

1. В настройках GitHub Pages введите домен в поле "Custom domain"
2. У регистратора домена добавьте DNS-записи:
   ```
   CNAME: www → YOUR-USERNAME.github.io
   A: @ → 185.199.108.153
   A: @ → 185.199.109.153
   A: @ → 185.199.110.153
   A: @ → 185.199.111.153
   ```
3. Подождите 24-48 часов для применения DNS

---

При возникновении проблем напишите issue в репозитории или проверьте [документацию GitHub Pages](https://docs.github.com/pages).

